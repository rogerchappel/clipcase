# ClipCase implementation tasks

## MVP complete

- [x] TypeScript CLI scaffolding and package metadata.
- [x] `init`, `new`, `add`, `list`, `show`, `search`, and `export` commands.
- [x] Local `.clipcase/` storage with deterministic JSON indexes and Markdown entries.
- [x] Secret blocking with `--allow-secret` override.
- [x] Fixture-backed unit tests and smoke test.
- [x] README, security, contribution, and CI updates.

## Future backlog

- [ ] Richer search output with snippets around matches.
- [ ] Optional JSON output for all commands.
- [ ] Configurable secret rules.
- [ ] Case rename and archive commands.
- [ ] Shell completions.
